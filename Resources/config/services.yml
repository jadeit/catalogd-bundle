parameters:
    jadeit.catalogd.event_subscriber.class: JadeIT\CatalogDBundle\Event\EventSubscriber

services:
    event_subscriber:
        class: %jadeit.catalogd.event_subscriber.class%
        arguments:
            - @logger
        tags:
            - { name: monolog.logger, channel: catalogd }
